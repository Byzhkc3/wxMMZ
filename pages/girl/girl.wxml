<view class="index">
  <view class="body">
    <view class="card" wx:for="{{resData}}">
      <view class="content">
          <image class="img_wrap" src="{{item.url}}"></image>
      </view>
    </view>
  </view>
</view>
